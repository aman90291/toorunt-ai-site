# sharp's postinstall downloads its prebuilt binary — approve it.
# `allowBuilds` is the pnpm 11+ key; `onlyBuiltDependencies` kept for older pnpm.
allowBuilds:
  sharp: true
onlyBuiltDependencies:
  - sharp
